package main

func main() {

	/*
			不同数据类型的零值不一样
			bool     false
			numbers  0
			string   ""
			pointer  nil
			slice    nil
			map      nil
			channel、interface、function    nil
		    struct 默认值不是nil，是结构体里面具体字段的默认值
	*/
}
